---
"@kekonic/diagrams-core": minor
"@kekonic/diagrams": minor
---

Edge label path bias via `labelPosition` / `labelAt` (`start` | `middle` | `end`; aliases `begin`/`source`, `mid`/`center`, `target`).
