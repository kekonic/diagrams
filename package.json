{
  "name": "@kekonic/diagrams-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run -r build && node ./scripts/deprecate-retired-packages.mjs --check && node ./scripts/check-release-workflow.mjs && node ./scripts/check-browser-bundles.mjs && node ./scripts/check-studio-bundle.mjs && node ./scripts/check-homebrew-formula.mjs && node ./scripts/check-docs.mjs && vp run diagrams#package && vp check && vp run -r test",
    "dev": "vp run @kekonic/diagrams-studio#dev",
    "docs": "vp run @kekonic/diagrams-website#dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "vp run -r build && changeset publish",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@kekonic/diagrams": "workspace:*",
    "vite": "npm:@voidzero-dev/vite-plus-core@0.2.2",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@11.11.0"
}
